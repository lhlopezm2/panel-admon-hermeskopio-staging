// Vacía a propósito — `necesidades_reportadas` hoy es insert-only (sin
// policy de SELECT para admin), así que todavía no hay nada que listar
// acá. Funcionalidad pendiente de definir.
export default function NecesidadesReportadasPage() {
  return (
    <p className="text-sm text-gray-500">Próximamente.</p>
  );
}
